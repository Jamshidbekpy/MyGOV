### Application / Database (used by docker-compose.yml)
# Copy to `.env` in a real deployment, or keep as-is and reference via `env_file: env`.
# Change passwords for production.

# App uses these env vars via `mysqli_connect.php`
MYSQL_HOST=l
MYSQL_DATABASE=l
MYSQL_USER=l
MYSQL_PASSWORD=l

### MariaDB container bootstrap
MARIADB_ROOT_PASSWORD=l
TZ=Asia/Tashkent
